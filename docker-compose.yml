version: "2"
services:
  serv:
    build: gril-app-server/.
    ports:
      - "5000:5000"
    depends_on:
       - db
  db:
    image: "postgres"
    ports:
      - "5432:5432"
    environment:
      NODE_ENV: "development"
      
 